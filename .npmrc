# Don't install package versions published less than this many days ago.
# Mitigates supply-chain attacks where a compromised version is published
# and pulled within a day or two before npm/the registry catches it.
# min-release-age=3
